namespace BinarySerializer.UbiArt
{
    public enum TextureType : byte
    {
        Generic = 0,
        BackLight = 1,
        BackLightEmmissive = 2,
        // TODO: For Adventures on Android there are additional values defined for compression types
    }
}